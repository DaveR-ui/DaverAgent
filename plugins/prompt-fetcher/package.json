{
  "name": "prompt-fetcher",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Exposes a fetch_original_prompt tool that retrieves the original human prompt from OpenCode's session DB via the plugin SDK.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@opencode-ai/plugin": "1.17.18"
  },
  "devDependencies": {
    "@tsconfig/bun": "^1.0.7",
    "bun-types": "^1.1.0",
    "typescript": "^7.0.2"
  }
}
