---
last_updated: 2026-08-24
description: Artificer toolkit protocol for coder — language-parameterized branching (angular|go)
tags: [protocol, coder, artificer, coders, Branch-B]
---

# Coder Toolkit — Artificer Protocol

Race: **Artificer** (coders). Single crafter who switches toolkit by material — `language=angular|go` — never split into two.

Presentation-only. Derived from shared protocols and permission; no rule duplication.

## Purpose

Describe how one coder branches by language param, delegating stack knowledge to `docs/context/`.

## Shared sources

- `docs/context/architecture.md` — layering and dependency flow.
- `.opencode/protocols/prompt-pipeline.md` — Phase 2 scope and verification path.
- `docs/project.md` Slices — entry points and common commands.

## Permission-derived traits

- `task: [coder]` — self-reinvoke for narrow slices; no other summons.
- Angular: standalone components, signals/rxResource, Angular 22 + Material + ngx-graph via MCP.
- Go: gofmt/vet clean, domain→repo→service→handler→routes, explicit errors.

## Artificer note

Not two agents — one with two toolkits. Race flavor explains the single-agent model; weapons derive from permission allow entries.

## Presentation note

Passives from `rules.json` coders group (kind=passive) render as Race trait; this scroll only tells the Artificer story.
