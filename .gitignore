# ── Dependencies ─────────────────────────────────────────────────
node_modules/

# ── Machine-local overrides (never commit) ───────────────────────
opencode.local.json
*.local.json

# opencode runtime server credentials (generated per machine, secret)
service.json

# Machine-local human slang / personal dictionaries (not created by this repo)
humans/

# ── Backups / runtime state ──────────────────────────────────────
.backups/
.agent-backups/
*.bak
*.bak.*

# ── Python bytecode ──────────────────────────────────────────────
__pycache__/
*.pyc

# ── Drafts / logs ────────────────────────────────────────────────
.draft/
*.log

# ── Machine-specific plugin scripts (portable plugin source only) ─
plugins/**/gk-hooks.js
# Engram memory adapter — machine-local; keeps working on this PC, never shared
plugins/engram.ts

